len=717

The user wants me to re-output Chapter 6 in the proper format (inside a code block with `章节` tag) and then call `preview_chapter` to save it. The previous attempts to output Chapter 6 weren't in the correct code block format.

Let me look at what happened - Chapter 6 was previously output but apparently wasn't in the right code block format for the system to save it. Let me re-output it properly now.

Looking at the context, I can see Chapter 6's full content is already present in the "前一章结尾" section. I need to put it in the proper format:

```章节
# 第6章 旧人

[content]
```

And then call `preview_chapter` with chapter_number=6.

Let me use the full Chapter 6 content that was already written, properly formatted.